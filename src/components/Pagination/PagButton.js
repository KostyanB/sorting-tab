import styled from 'styled-components';
import MainButton from '../Styled/MainButton';

const ButtonPagination = styled(MainButton)`
    padding: 10px;
    border-radius: 5px;
`;
export default ButtonPagination;